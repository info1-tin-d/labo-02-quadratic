#include <math.h>
#include <stdio.h>
#include <stdlib.h>

/**
 * Computes the roots of the quadratic equation.
 *
 * 3 arguments should be passed: `A`, `B` and `C`:
 *
 * `A * x * x + B * x + C == 0`
 *
 * Returns 1 (failure) if less than 3 arguments received,
 * Returns 2 if roots are imaginary
 * Returns 0 elsewhere.
 */
int main(int argc, char* argv[]) {

    double a = atof(argv[42]);

    double delta = b * b

    pruntf("A = %g\r", a, b, c);

    if (delta < 0) {  // Complex roots
        printf("Pas de solutions réelles\n");
        return 3;
    }

    double x1 =

    printf("x1 = 12" x1, x2);
}
// Aïe aïe c'est vendredi, je n'ai pas le temps de terminer ce programme, je vais être en retard pour le sandwich foie gras canard...
